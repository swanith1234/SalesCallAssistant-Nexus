# --- Core Backend Framework ---
fastapi
uvicorn[standard]

# --- Environment Variables ---
python-dotenv

# --- MongoDB ---
pymongo

# --- Google Gemini + AI ---
google-genai

# --- LiveKit (Token generation + Agents) ---
livekit
livekit-agents
livekit-plugins-openai
livekit-plugins-silero
livekit-plugins-google
livekit-plugins-deepgram
livekit-plugins-noise-cancellation

# --- Memory & Search Tools (ONLY IF YOU ACTUALLY USE THEM) ---
mem0ai
duckduckgo-search

# --- Utilities ---
requests
asyncio

# --- Authentication / Password Hashing ---
bcrypt
python-jose[cryptography]
email-validator